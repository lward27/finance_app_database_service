Python 3.11 FastAPI database service for the Finance Product. Source lives under src/finance_app_database_service. Keep input validation pure and testable before database access; maintain stable FastAPI behavior and SQLModel boundaries. Add standard-library tests under tests/ and update README.md when behavior changes. Use only the declared acceptance commands. Dependencies are installed only during PHarness preparation from the immutable hashed requirements.lock; coding-stage network and package installation are denied.